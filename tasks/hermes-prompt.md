# Hermes prompt — tasks 31-80

Copy-paste this into Hermes/Nemotron:

---

Read `tasks/hermes-rules.md` first — it has mandatory build, comment, and type rules. Then work through the task files one at a time in order:

`tasks/task-31.md` through `tasks/task-80.md`

For each task: read the file, do exactly what it says, run `npx tsc --noEmit` before moving to the next. If the build fails, fix it before starting the next task. Do not skip a task. Do not add comments explaining your changes.
